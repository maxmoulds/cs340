<html>
{{#each foo}}
{{this}}
{{/each}}
<h3>Yo these are them classes:</h3>
<form id="classtable" action="/classes" method="post">
    First name: <input type="text" name="fname"><br>
    Last name: <input type="text" name="lname"><br>
    Role: <select name="role_id"> 
        {{#each all_role_ids}}
        <option value="{{role_id}}">{{role_id}}</option>
        {{/each}}
    </select><br>
    Age: <input type="number" name="dob"><br>
    <input type="submit" value="Submit">
 </form>
<br/>
<h3>Current character Class list:</h3>
<table>
    <thead> 
        <th>First Name</th> 
        <th>Last Name</th> 
        <th>Role</th>
        <th>dob</th>  
    </thead> 
    <tbody> 
        {{#each courses}} 
        <tr> 
            <td>{{id}}</td> 
            <td>{{description}}</td> 
            <td>{{instructor}}</td> 
            <td>{{subject}}</td>
            <td><button onclick="deleteCharacter({{id}})">Delete</button></td>
            <td><a href="/classes/{{id}}">Update</a></td>
        </tr> 
        {{/each}} 
    </tbody> 
</table> 
</html>
